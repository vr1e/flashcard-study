{
	"name": "flashcard-study",
	"version": "1.0.0",
	"description": "Flashcard study tool with spaced repetition algorithm",
	"scripts": {
		"build": "tsc",
		"watch": "tsc --watch",
		"dev": "tsc --watch",
		"postinstall": "npm run build"
	},
	"keywords": [
		"flashcards",
		"spaced-repetition",
		"learning",
		"education"
	],
	"author": "",
	"license": "MIT",
	"comment": "Chart.js, qrcodejs, and Bootstrap are loaded via CDN in templates/base.html, not bundled, so they are not npm dependencies. TypeScript is the only build-time requirement.",
	"devDependencies": {
		"typescript": "^5.9.3"
	}
}
